- name: Purge jsDelivr cache
  # You may pin to the exact commit or the version.
  # uses: gacts/purge-jsdelivr-cache@ec03611302454cf4909c7a0e0fd3ac50f2bdb53c
  uses: gacts/purge-jsdelivr-cache@v1.0.1
  with:
    # URLs for the cache purging (string|list)
    url: https://cdn.jsdelivr.net/gh/arskolyba/companiestools/code.min.js
    # Retry attempts (on the request failing) (integer)
    attempts: # optional, default is 3
